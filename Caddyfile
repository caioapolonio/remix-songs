# Endereço do site vem de SITE_ADDRESS:
#   - Local (validação):  :80          → HTTP, sem TLS (não há DNS público)
#   - Produção (VPS):      remix-songs.com → TLS automático via Let's Encrypt
{$SITE_ADDRESS} {
	reverse_proxy next:3000
}
